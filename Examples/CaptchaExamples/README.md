# Captcha examples

Each supported captcha has its own folder and standalone `simple.py`.

Run one example from the project root:

```powershell
python Examples/CaptchaExamples/TextCaptcha/simple.py
```

Every visual example writes files into its local `output/` directory. Open each
`simple.py` to see the settings supported by that generator.
